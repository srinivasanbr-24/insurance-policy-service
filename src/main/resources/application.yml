spring:

  application:
    name: insurance-policy-service

server:
  port: 8080

management:

  endpoints:
    web:
      exposure:
        include: health,info,metrics

springdoc:

  swagger-ui:
    path: /swagger-ui.html

  api-docs:
    path: /v3/api-docs